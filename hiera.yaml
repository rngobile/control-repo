---
version: 4
datadir: hieradata
hierarchy:
        - name: "Environment Data"
          backend: yaml
          paths:
                - 'nodes/%{trusted.certname}'
                - 'nodes/%{facts.fqdn}'
                - 'nodes/%{facts.hostname}'
                - '%{facts.os.family}'
                - '%{facts.os.name}'
                - 'default'

